# UI 维护约定

- 用户要求：补充解释、快捷键说明和使用提示统一使用 `src/HelpHint.jsx` 问号组件，不在操作区堆叠大段小字。字段名称、必要的当前状态、错误和操作结果仍直接显示。
- 帮助提示必须支持鼠标悬停、点击、键盘和触屏点击；点击打开后不能被触屏松手产生的离开事件立即关闭。长说明在小屏上应可阅读和滚动。
- 新增或修改交互需验证对应的桌面与触屏路径，包括关闭、边界和失败恢复；不能只验证鼠标操作。
- 大图查看器同时保留翻图、缩放和平移。翻图沿用当前知识库、筛选结果或笔记配图顺序，不能跳入其他知识库。
- 图片组在多选和普通浏览时保持相同的折叠展示；选择操作不能隐式拆组。组卡片选择整组，单张选择使用组内选择窗口，只有明确移出或解散才改变分组。

# 交付与验收约定

- 不能把临时测试数据库、单元测试或构建成功直接当作用户问题已解决。涉及运行时、采集、预览、排序、移动端或插件的改动，必须再检查当前实际运行的服务/端口、真实数据和页面交互。
- 报告“完成”前至少要核对：代码测试、生产构建、当前实例接口、当前实例 UI；交互改动还要覆盖桌面与触屏、关闭、边界和失败恢复。任一环节未验证，要明确写出未验证范围，不能擅自报达成。
- 能从本地服务、数据库、日志、浏览器请求和自动化页面检查得到的证据，应由开发者先检查，不把截图或重复复现工作转嫁给用户。
- 发布后要确认运行实例已经重启/加载新构建，并核对提交已经推送到远端；旧页面可能缓存旧脚本时，需明确提示重新加载。
- 长期产品需求和已踩坑记录维护在 [docs/PROJECT_MEMORY.md](docs/PROJECT_MEMORY.md)，接手任务前先阅读相关章节。
