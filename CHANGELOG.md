# 更新记录

## 未发布 · Android 采集收起与抖音实况视频

- 抖音实况图直接保存为可播放视频；同一作品含多段实况时分别入库并保持作品顺序，不再额外保存重复的静态封面。
- 点击“采集当前作品”后悬浮入口立即收起，提交后台任务后小面板自动关闭，浏览过程不再被采集状态遮挡。
- “分享 → 复制链接”识别支持原 App 自有面板和系统分享窗口，继续按小红书、抖音与 B 站来源域名校验剪贴板，避免误采旧链接。

## 未发布 · 轻量恢复快照与按需迁移

- 自动和手动备份改为恢复快照：SQLite 使用一致性副本，同盘图片和视频使用硬链接复用；不支持硬链接时自动退回复制，并显示实际新增占用。
- 迁移 ZIP 改为点击时流式生成并直接下载，不在服务器长期保存整包；服务器快照可直接检查、恢复、导出或删除，旧版完整 ZIP 保持兼容并随保留策略轮换。
- 服务启动会清理异常退出留下的临时备份；迁移 ZIP 上传上限提高到 25 GiB，恢复仍校验数据库、媒体哈希和解压总量。

## 未发布 · 源码与文档集中整理

- 新增 `addons/` 总入口，浏览器扩展、外部连接器和 AI 工作流分别归入 `browser/`、`connectors/` 与 `ai/`。
- 新增扩展与文档索引；构建脚本、测试、许可证链接和插件市场路径统一迁移，私人插件构建镜像移出公开工作区。
- 私人浏览器扩展统一放入本地 `addons/browser/private/` 嵌套仓库，公开 Git 整目录忽略源码、安装包和构建产物。

## 未发布 · 导航卡片计数

- 全部内容、收藏和知识库数量按折叠卡片统计：图片组计一张，单图、视频和笔记各计一张；排除回收站并隔离知识库。
- 图片素材保留原始图片张数与卡片数两项，API 同时保留原始计数与新增卡片计数。

## 未发布 · 批量上传图片组

- 多张图片上传默认组成一组，可取消勾选分别入库；第一张提供封面和组名，无需额外命名。
- 上传队列固定知识库、标签与分组，失败重试保留组和顺序；复用已有文件时保留原图片组。

## 未发布 · 紧密视图与笔记封面

- 新增紧密网格和紧密列表，支持知识库浏览记忆、常用筛选和手机布局，保持图片组折叠与多选行为。
- 图文笔记使用正文第一张本地图片的缩略图作为封面，支持长正文和 Markdown 引用图片；封面不引用回收站或其他知识库图片。

## 未发布 · 预览标签检索

- 图片、视频和笔记预览中点击标签检索当前知识库的同标签资源，只有点击 X 才移除标签。
- 修复标签文字触发表单默认删除按钮的问题，补充桌面与触屏检索、删除验证。

## 未发布 · Windows 无窗口后台启动

- 修复默认终端为 Windows Terminal 时，PowerShell 隐藏参数仍会弹窗的问题；改用系统 headless 控制台宿主，保留退出日志和自动恢复。
- 补充实际计划任务启动、桌面窗口检测和宿主停止后的子进程回收验证。
- 修复计划任务直接启动 Node 控制台，关闭终端窗口就导致知识库中断的问题。
- 改用无窗口启动器，统一管理 Node 子进程；停止任务不遗留旧服务，保留原有自动恢复。
- 新增启动器日志，记录 Node 原始退出码与启动失败，辅助判断进程被外部结束的情况。

## 未发布 · 采集与图片统计

- 图片素材同时显示图片张数与独立卡片数（图片组 + 单张图片），按知识库统计并排除回收站。
- 浏览器工具 0.9.16：预览中可选择并记住目标知识库，批量入库沿用目标直接开始，无需二次点击保存。
- 批量任务固定目标库；刷新、停止、失败恢复保留进度，重新打开不会自动重复提交。

## 未发布 · 单张组展示

- 只有一名成员的图片组按普通图片展示，隐藏组标记和组内操作，预览沿用当前筛选的图片顺序。
- 保留作品关联，后续补图时自动恢复组展示；标签、搜索只匹配到一张时，原多图组仍保持折叠。
- 完整组人数按知识库和回收站状态隔离，多选、收藏与预览保持一致。

## 未发布 · 插件独立维护

- 设置页改为通用「其他插件」接入说明，继续使用支持触屏的问号提示。
- 专用插件的源码、构建和安装包移出后续公开版本；移除专用下载接口与客户端内附安装包。
- 保留已有数据格式和 API 兼容性，已入库资料及已安装插件的连接不受影响。

## 未发布 · Pixiv 入库自动重试

- Pixiv 增强版升级至 1.3.0：断网、超时、限流和临时服务器错误最多自动重试 4 次，逐步延长等待并遵守服务器重试时间。
- 成功文件直接跳过；等待任务释放队列供其他任务继续，重试次数与时间持久保存，支持后台恢复和停止。
- 收起队列也显示失败和等待重试数量，提供「查看失败」「重试失败任务」，包含需要处理的收藏同步失败。
- 权限、文件不存在和大小限制等错误保留明确原因；上传成功但响应丢失时重试仍按原文件或小说来源去重。

## 未发布 · Windows 后台运行

- 增强退出诊断：运行状态每 30 秒落盘，记录退出码、关闭信号及异常调用栈，重启时保留上次缺失退出记录的证据。
- 增加排除环境变量的异常和内存耗尽诊断报告，并限制历史报告保留数量。

- 新增 Windows 后台任务安装脚本：登录自动启动，独立于终端运行，异常退出后自动重试，并保留启动及退出日志。
- 部署指南补充后台服务的安装、停止、更新与卸载方法。

## 未发布 · 卡片收藏按钮统一

- 修正收藏底栏的垂直对齐：日期、尺寸与星标居中，上方分隔线保留统一留白，窄屏文字可换行。

- 单张图片和图片组的收藏按钮统一固定在卡片右下角，独立于整组选择按钮，不再随分组操作移到图片上方。
- 收藏按钮增加不透明底色、边框和明确的选中状态，兼容明暗主题、网格、列表及触屏。

## 未发布 · 图片组自动命名

- 创建图片组不再要求填写名称；留空或只输入空格时，自动采用整理后封面图片的标题，预览直接显示最终组名。
- 整组展开、跳过笔记配图时，以实际首图命名；手动名称仍优先，追加到已有组保留原名称。

## 未发布 · 多选保留图片组

- 进入多选保留图片组、笔记配图组的折叠卡片与浏览位置，不再切换成逐张列表。
- 点击组卡片选择整组；当前页全选、反选和 Shift 连续选择按组处理，筛选全选仍只包含匹配内容。
- 新增分页的组内选择窗口，支持触屏挑选、取消和部分选中状态；选择本身不改变分组、顺序或封面。
- 整组读取失败或切换知识库时不提交部分选择，继续沿用批量操作的版本检查与撤销保护。

## 未发布 · 触屏帮助与大图连续浏览

- 问号说明支持触屏轻点、鼠标悬停和键盘打开；点击后保持显示，再次点击、点击外部或返回可关闭，长说明可在小屏内滚动。
- 将微信收件、通知、客户端和助手连接的补充说明统一收进问号提示，并记录后续 UI 维护约定。
- 展开大图后支持 A / D、左右方向键、两侧按钮和触屏滑动翻图，沿用当前图片组或笔记配图顺序；放大后先平移，到边缘后继续向外滑动翻图。
- 修复连续展开浏览时底层窗口抢焦点；手机从导航打开设置后收起导航遮罩。

## 未发布 · 草稿提示布局与失败恢复

- 草稿冲突提示补齐左右留白与内部间距，窄屏按钮自动换行，保持正文和底部操作可用。
- 丢弃本地草稿失败时保留对照窗口与错误提示，允许重试；处理期间禁用重复操作。

## 未发布 · 微信消息块与笔记预览

- 微信消息按块预览，补充连续空行显示；保留原始换行、Markdown 链接及代码块。
- 每条消息可独立编辑、复制、剪切、删除和撤销，支持桌面与触屏拖动排序、键盘和上下移动按钮。
- 新收件使用可随 Markdown 导出与版本恢复的明确消息边界，兼容旧版分隔线笔记；沿用本地草稿、版本冲突保护和配图分组。
- 笔记工具栏将编辑/预览与其他操作分组，统一按钮尺寸、图标和间距，窄屏自动收起操作文字。
- 增加独立微信通知接口与仅通知令牌，默认关闭发送；复用原绑定、隔离收件流程，支持按事件编号去重及明确的发送结果记录。

## Android 0.10.0-beta.2 · 分层返回

- 系统返回键及安卓返回手势优先关闭帮助、大图、图片详情和弹窗，再返回之前的知识库视图；最外层才将应用放到后台。
- 返回时保留现有笔记草稿与未保存修改确认，顶部返回按钮使用相同逻辑。
- 网页提供通用外部助手入口，仅传递当前知识库编号；助手服务独立部署，本仓库不包含其实现。

## 未发布 · 设置排版、微信消息与随手多选

- 设置中浏览器扩展、Pixiv 和客户端说明采用独立分区与统一间距，支持窄屏和明暗主题。
- 微信合并笔记的新消息之间加入 Markdown 分隔线；笔记预览保留单次回车换行，链接、列表和代码块保持正常。
- 向下浏览后显示悬浮多选入口，进入选择保留浏览位置，批量管理栏置顶；电脑和手机都无需返回页首。

## 0.10.0-beta.1 · 客户端与公开维护入口

- 自有代码采用 GPL-3.0-or-later，保留 Pixiv 增强版及第三方许可和对应源码。
- 新增 Windows / macOS 客户端：记住连接地址、内置独立 Node 服务、托盘驻留、可选视频组件；复用已有服务时不迁移原数据。
- 新增 Android 连接客户端：多文件选择、预览、系统下载和 Markdown 保存；不提供手机整库离线同步。
- 网页增加主屏幕图标、安装信息和公开断线提示，Service Worker 不缓存私人 API、笔记和原图。
- 设置页增加客户端下载与反馈入口；提供问题模板、贡献指南、安全报告说明及跨平台构建工作流。
- 数据 schema 13 不变；原服务、扩展连接方式与知识库数据目录保持原状。
- 修正不可下载的间接依赖；适配新版 FFmpeg 对本地 HLS 分片后缀的检查，同时保留包内路径及媒体格式限制。


## 0.9.31 · 微信图文按天归档

- 微信收件默认按北京时间的消息日期合并；分开发送的文字、图片和说明持续追加到同一篇 Markdown 笔记，配图统一成组。
- 提供手动持续收集与原逐条模式；设置中「开始新篇」或微信单独发送 `/新篇 主题名` 可切换话题，当前笔记和收件记录可直接打开内容。
- 追加读取最新正文，保留手工标题、标签和封面顺序；排队时固定归档目标，旧笔记不自动合并。
- 笔记写入与完成回执原子提交，清理近期记录、重复推送与恢复后不重复追加。同篇失败先处理，后续消息等待，失败记录优先展示。
- 删除或移动笔记后新消息另起一篇；旧队列不会复活被删除的笔记。完整备份包含分篇状态与去重回执，schema 13 不变。

## 0.9.30 · 更顺手的多选与批量整理

- 多选工具栏适配桌面与手机，滚动时保持可见；提供筛选结果全选、已加载反选、一键收藏和整组选中 / 取消，处理期间统一禁用冲突操作。
- Ctrl / ⌘ 点击直接进入多选，Shift 连选，Ctrl / ⌘ A 全选，Esc 清除 / 退出；输入框与弹窗不会误触列表快捷键。
- 全选覆盖未加载内容，分页读取保持同一事件游标，显示进度并支持取消；失败或超过 10000 项时保留原选择，切换范围时取消旧请求。
- 批量改标签、收藏和移动后同步未显示条目的版本，支持连续整理；删除和恢复继续使用事务与撤销。
- 进入多选保留浏览锚点；修复切换知识库后标签等区域延迟渲染带来的小幅位置漂移，用户开始滚动时立即交还控制。
- 原文件、笔记分组、schema 13 与浏览器扩展连接不变。

## 0.9.29 · 微信收件箱

- 设置中新增个人微信扫码连接、验证码确认、默认知识库与标签、暂停/恢复和断开入口。通过腾讯 iLink 长轮询接收文字、图片和同条图文消息，默认不向微信回复。
- 图片下载解密后按原文件保存，同条图文消息生成 Markdown 笔记和配图组；按消息编号去重，保留 64 位编号精度，异常中断后跳过已经保存的内容。
- 持久化接收队列和游标，支持失败重试/忽略；连接和收件状态纳入完整备份。管理员可查看收件记录，过滤非绑定账号与群消息。
- 未连接时不启用网络接收，不改变已有知识库和扩展连接；微信账号需要支持该入口并由本人扫码确认。

## 0.9.28 · 批量入库留在当前网页

- 普通扩展更新为 0.9.15：「批量入库」打开网页内的自适应小浮窗，不再跳转独立作品页；批量下载入口保留。
- 浮窗支持知识库与标签选择、逐页进度、停止和失败重试；收起仍继续，多个作品可切换查看。重复点击正在保存的作品会展开原任务。
- 刷新原网页后找回未完成任务，手动继续并跳过成功项；保留原图、作品分组、页序与首图封面。更新扩展保留已有连接配置。
- 表单和 API 连接在扩展自身页面内处理，宿主网页只能接收进度；不新增轮询或扫描。

## 0.9.27 · 批量采集成组修复

- 普通扩展 0.9.14 为 Paw、Pixiv 及通用网页图集批量入库补齐分组标识、标题和从零开始的页序。作品身份不依赖任务 ID、标题或跟踪参数；Paw 两个域名沿用相同作品标识，首张作封面。
- 修复成组去重：相同原文件的不同页保留独立记录；重试保持页身份，不抢占另一个自动图集的成员，保留已有手工组与笔记归属。Pixiv 增强版原有作品分组继续兼容。
- `POST /api/assets/batch` 提供非空分组标识时，按文件顺序从指定起始页码递增；部分失败不会改变后续原始页序。没有分组标识时保留原上传行为。
- 提供保守的旧批次修复工具：仅恢复来源和自动备注可完整核实、未编辑的 Paw / Pixiv 批次；完整备份后停服应用。重采集也可复用符合条件的旧记录；不复制原文件。
- 扩展下载包文件名读取实际版本，避免继续显示过时版本号。schema 13 不变，扩展固定 ID 和连接设置不变。

## 0.9.26 · 手机连续看图

- 图片组和笔记配图支持单指左右滑动翻页，轻点展开；竖向滚动、多指手势和取消操作不误翻页，滑动后的兼容点击不误展开。保存失败仍留在原图。
- 展开后支持双指围绕触点缩放、单指拖动和双击放大 / 复位。松开一根手指后可连续平移；缩放限制 25%–800%，限制拖动边界，横竖屏变化重新适配。
- 点击图片外空白处退出，拖动或双指操作不误关闭。保留电脑滚轮与翻页快捷键，增加 `+` / `−` / `0` 缩放控制，Tab 限定在展开层，Esc 只退出当前展开层。
- 原图加载失败提供重试；触屏按钮扩大点击区域并避让屏幕安全边距。动画帧合并缩放绘制，没有后台轮询。
- 使用 Edge 触屏模拟验证；尚未进行实体 Android / iOS 设备验收。数据格式仍为 schema 13，原文件和扩展不变，刷新知识库网页即可使用。

## 0.9.25 · 视频继续观看

- 视频素材新增播放记录，每库最近 20 个视频；打开预览可明确选择上次位置或从头查看，历史入口直接定位，不自动播放声音。
- 播放时约每 10 秒同步，拖动、暂停、页面隐藏与关闭预览时保存；仅预览不写回零秒，看完与重新观看区分。删除或移动的视频不跨库显示，取消待同步位置，原文件不复制、不修改内容时间。
- 复用单请求同步与本地待确认请求机制，图片与视频暂存分开；失败后保留原请求标识，跨刷新可重试，多设备版本冲突不会静默覆盖。
- schema 13 增加独立的 `video_progress` 表；旧数据增量升级，已有图片浏览记录保持不变。完整备份校验并恢复视频记录，恢复后旧待同步请求失效。
- 浏览器进程崩溃、强制结束或突然断电可能丢失尚未捕获的位置；本地未同步记录不随服务器备份迁移。本次不增加转码、自动播放或下载副本，扩展无需重装。

## 0.9.24 · 大文件流式导出

- 六种导出改由浏览器直接接收 HTTP 下载流，不先在网页内读取完整 Blob，也不额外保存服务器 ZIP 副本；支持局域网 HTTP，无需文件系统权限或额外扩展。
- 任务中心显示服务器排队、打包与传送、失败和取消状态。页面刷新后仍可查看，断线或缺失原文件可重试并保留原导出范围与格式。最终保存结果由浏览器下载列表确认。
- 原生导出队列串行执行，至多 20 个未结束请求；未开始下载的回执 5 分钟后过期，结束记录至多 50 条、保留 24 小时。回执重试不重复派生后续任务，服务器重启或完整备份恢复清除旧回执。
- 修正打包库提前读取所有媒体的问题：逐条加入媒体流，最多读取一份原文件；慢速接收保持背压，断线或取消释放流。完整备份避免预读一份无用的全部内容，导出范围与附件匹配改用集合查找。
- 原有导出 API 和六种格式保留，数据仍为 schema 12。上传跨刷新恢复、断点上传与中断后的 ZIP 续传尚未实现；导出重试会重新生成，文件以重试时的数据为准。扩展无需重装。

## 0.9.23 · 统一任务中心

- 右上角新增任务入口与进行中数量，汇总上传、网络采集、导出和服务器备份；可按知识库、进行中或需要处理筛选。
- 图片和视频上传共用页面队列，串行发送；导出独立串行。关闭上传、导出弹窗后继续执行，可在任务中心取消、重试或打开结果。上传完成发送后显示验证入库阶段，不把 100% 发送进度当作入库成功。
- 本地待处理及失败任务最多 1000 个，已成功记录保留最近 100 条；任务列表每页 40 条，上传列表每页 50 个。清除当前范围本地结束记录释放文件引用，不删除知识库内容。
- 采集重试沿用原来源、知识库与标签；返回后续任务 ID，重复提交同一旧记录不重复建任务。已经重新提交的旧失败记录不持续提示失败。
- 服务器状态由统一轮询维护，活动时加快、空闲时降低频率，页面隐藏时暂停。任务完成后刷新内容，失败可手动重试。
- 手动备份及服务器备份文件在任务中心可见，完整备份标为全部知识库；导出完成表述为「已交给浏览器下载」。修正导出窗口中过时的恢复说明。
- 数据格式仍为 schema 12；本地上传和导出尚不支持刷新后恢复，独立视频预览转码与流式大文件导出仍待完善。扩展无需重装。

## 0.9.22 · 大图库标签搜索

- 标签筛选改为整库搜索、每页 40 个及使用次数；跨页选择独立保留，支持全部 / 任意匹配。
- 侧栏只请求 30 个常用标签，独立于内容列表加载；搜索内容或切换筛选不反复下载整个标签词表。
- 笔记、上传和批量标签输入按关键词请求推荐，可搜索冷门作者；支持下方向键进入推荐按钮、回车选用，不再把搜索片段一起保存为标签。
- 取消过期搜索和切库请求，失败提供重试；标签翻页遇到内容更新时提示刷新，已选条件保留。
- 手机筛选工具改为紧凑横排。API 保留默认数组响应，新增可选分页、字面搜索与游标校验；数据格式仍为 schema 12，扩展无需重装。

## 0.9.21 · 图片组排序撤销

- 拖拽排序接入「最近操作」，预览底部可直接撤销，关闭或刷新后仍能找回。
- 一起恢复展示顺序、首图封面与本次同步的笔记正文，保留 Markdown 链接及无关的标题、标签和收藏修改。
- 检查完整组成员与顺序，包括本次未移动的图片；后续增删、移动、重新分组或排序冲突时整次拒绝。连续排序可按相反顺序逐次撤销。
- 撤销响应丢失可重试，不重复写入；撤销期间输入的笔记文字保留在编辑器中。
- 修复保存按钮暂时禁用或消失后焦点落到页面、Esc 无法关闭预览的问题；只关闭最上层窗口。
- API 排序可选 `undo:true`，数据格式仍为 schema 12，不复制原文件；完整备份恢复后旧撤销记录作废。扩展无需重装。

## 0.9.20 · 跨设备继续看图

- 各知识库显示「继续查看」与「浏览记录」，保留最近 20 个图片组或单张图片的位置；另一设备登录同一服务可接续。
- 记录图片 ID，排序变化后重新计算页码；同组保留最后一张，删除、移动或重新分组后不显示失效记录。普通点图仍打开指定图片。
- 笔记配图也记录位置；浏览不修改内容、更新时间或内容事件，不额外复制或保留原文件。
- 合并快速翻页写入、单请求串行同步。失败请求本地暂存，刷新后可重试；旧请求不能覆盖其他设备的新位置，也可放弃本次同步。
- 新增读写 / 清除 API 与 schema 12；完整备份保留浏览记录，恢复后更换同步标识，旧待提交请求须重新确认。
- 桌面和手机均可查看、清除记录；清除不删除图片，普通扩展无需重装。

## 0.9.19 · 大图库连续浏览

- 接近列表底部自动加载下一批，可切回手动加载；失败后暂停并提供重试，内容更新时提示刷新，避免偏移分页跳过内容。
- 列表保留附近最多 600 项摘要，远处卡片不挂载；完整正文只在打开详情时读取。已选内容单独保留，移出浏览缓存后仍可批量操作。
- 续页、缓存替换和向前加载保持可见卡片位置，避免浏览器追随页尾而持续触发请求；切库取消过期响应。
- 新增可选 summary / cursor 查询参数，保留旧 API 默认完整响应；打开详情先核对归属和删除状态，不以摘要编辑原文。
- 手机的结果操作栏保持完整文字；显示当前浏览范围，滚动加载偏好保存在当前浏览器。
- 数据格式保持 schema 11，普通扩展无需重装。

## 0.9.18 · 图片组合并、追加与拆分

- 多选图片后进入「整理图片组」，支持仅所选或包含整组，预览实际范围与首图；追加时保留目标封面和已有顺序。
- 笔记配图可跳过，或保留原笔记和配图组并建立素材引用；共享原文件，不重复存图。素材组名称可搜索。
- 手工整理标记与来源页码分开保存，重复采集不覆盖整理结果。支持操作撤销，组成员或版本变化时整次拒绝，防止部分更新。
- 提交操作保存回执，网络响应丢失后重试不重复建组；桌面和手机界面提供预览、范围提示及明确的冲突恢复入口。
- schema 11 增量升级，完整备份保留分组信息，恢复后清除旧操作回执；单次关联及结果组最多 10000 张，预览最多 60 张。
- 普通扩展仍为 0.9.13，无需重装。

## 0.9.17 · 跨设备常用筛选

- 保存分类、关键词、标签组合、排序与布局，侧栏一键切换；匹配新入库内容，沿用现有搜索与分组规则。
- 常用筛选按知识库隔离，其他设备登录同一服务可用；支持重命名、替换条件、删除定义，保留原内容。
- 多窗口修改与删除检查版本；冲突时保留输入并可读取最新版本。切库时取消旧请求，网络错误可重试，不重置当前浏览条件。
- 新增读写 API、OpenAPI 文档及 schema 10；完整备份保留定义，旧备份仍可恢复。每库最多 50 个，删除知识库时清理其专属定义。
- 弹窗中 Ctrl / Cmd + K 不再将输入焦点带到背后的搜索栏；粘贴图片不会打开额外上传弹窗。
- 普通扩展仍为 0.9.13，无需重装。

## 0.9.16 · 笔记草稿与版本找回

- 自动保存本地草稿，恢复编辑正文、光标和滚动位置；按知识库找回未提交的新笔记。已打开页面可离线续写，正文可直接导出 Markdown。
- 多窗口分别存草稿；遇到服务器较新版本时提供正文对照。保存途中继续输入不被响应覆盖，支持 Ctrl / Cmd + S。
- 草稿写入失败时显示实际状态并保护关闭操作；不把本地草稿当作服务器已保存。
- 增加笔记版本记录及读取 API；旧版本先载入编辑器，手动保存仍检查版本与配图状态。
- schema 9 保存压缩历史，每篇最多 50 个版本、8 MiB；完整备份保留历史，永久删除笔记时清理。旧图片文件不会因历史记录额外保留。
- 普通扩展仍为 0.9.13，无需重装。

## 0.9.15 · 可撤销的整理操作

- 普通删除、批量标签、移动、收藏及已有内容编辑增加撤销，包含一起变化的笔记和配图。
- 新增快捷「撤销」和「最近操作」：刷新页面或重启服务后继续使用，本次登录保留最近 24 小时、最多 50 条记录。
- 仅还原操作修改的字段，保留后续无关修改；冲突、原知识库缺失、永久删除或引用关系变化时整次拒绝并说明原因。
- 撤销删除会还原原图引用并清理本次生成的独立配图记录，原文件继续由引用检查保护；永久删除不可撤销。
- schema 8 使用压缩字段差异并限制历史大小，不额外复制原图；支持旧备份，完整恢复后作废旧撤销记录。
- 普通扩展保持 0.9.13，无需重装。

## 0.9.14 · 连续浏览与批量选择

- 各知识库及内容分类分别记住搜索、标签、排序、布局和浏览位置；重新打开默认知识库可继续浏览。
- 按上次浏览内容定位分页，支持向前、向后加载；大列表只渲染视口附近卡片，移除会造成位置漂移的旧占位高度。
- 返回窗口时提示内容更新，不再重载第一页或清空选择；主动刷新也保留所选内容及其版本信息。
- 支持 Shift 连选 / 取消已加载范围；保留整组选中、跨页整理及知识库隔离。
- 防止重复分页请求和切库后的旧响应混入；新增兼容原查询的 anchor 定位与最新事件游标 API。
- 普通扩展保持 0.9.13，无需重装。后续全项目体验优化见 [优化路线](docs/EXPERIENCE-ROADMAP.md)。

## 0.9.13 · 降低嗅探 CPU 与视频加载开销

- 修复扫描重建卡片、封面视频重新加载、网络事件再次扫描形成的循环；未变化的卡片保持原 DOM。
- 保留站点封面；缺少封面时仅在悬停/聚焦卡片后取帧，同一页面最多一个取帧任务，完成后释放解码器并缓存小图。
- 自动扫描合并排队，过滤无关图片/样式请求，只保存最近 200 个候选地址；后台标签页不执行扫描或页面数据读取。
- 抖音页面属性仅在打开、手动刷新或页面切换时检查，并缓存已经读取的属性；列表轮询不再重复写入存储。
- 同一 24 条视频测试中，静置 8 秒的重复扫描从 15 次降为 0，额外视频请求从 45 次降为 0。该数据为受控对比，不代表所有网页的整机占用。

## 0.9.12 · 动态视频作者与封面嗅探

- 修复抖音连续浏览时，后续作品只有标题、没有作者标签的问题：匹配动态接口及已挂载作品数据，入库前补充最新作者信息。
- 视频嗅探显示作品封面；无封面的普通视频按可见范围加载首帧预览，点击封面可播放。
- 同一抖音作品的多条播放线路合并成一张卡片，保留线路选择、下载和入库；不按相似标题合并不同作品。
- 保留网站黑名单、暂停嗅探和原有连接设置；页面数据桥仅传递作品元数据，不传递响应正文或请求凭据。

## 0.9.11 · 抖音作者标签

- 扩展补充抖音详情、信息流与个人页弹层的作者识别，解析作品编号与页面作品数据，将昵称写入标签并保留作者主页/作品来源。
- 优先匹配对应视频资源，过滤评论区、导航和“我的”等按钮，避免用当前作者标记其他预加载视频；支持作者较晚出现后补全。
- 支持播放器追加参数及 CDN 切换后的资源匹配。带 `modal_id` 的个人页/收藏页链接自动转换为对应作品链接。
- 链接采集优先使用抖音展示昵称，不优先使用数字账号或账号名。已有视频再次入库可补标签，原备注和原文件保持不变。
- 普通采集扩展更新至 0.9.11，身份、API 连接与个人设置保持。

## 0.9.10 · 一键选择整组图片

- 分组卡片与图片预览新增「选择整组」，包含当前库同组所有图片，不受筛选或已加载页数限制。
- 保留逐张取消、加选其他组和清除选择；当前页复选框仅影响已加载内容，部分选择显示半选状态。
- 批量标签、整理、删除、恢复和永久删除支持最多 10,000 项，并保留原子版本校验。完整选中笔记配图后移动，所属笔记同步移动。
- 切换知识库后丢弃迟到的选择请求，避免选择串库。

## 0.9.9 · 删除笔记后保留图片组

- 修复 0.9.8 永久删除笔记时清空配图分组、导致几十张图片散开的回归。
- 保留的配图转为普通图片组，保留组名、原始页码、手动排序及首图封面；继续支持整组移动、收藏与排序。
- 提供基于删除前备份的定向修复脚本，仅恢复未被再次编辑的受影响分组，不恢复已删除的笔记或图片。

## 0.9.8 · 完整回收站与独立笔记配图

- 新增单项永久删除、永久删除所选、清空当前知识库回收站；确认实际数量，清空不受搜索、标签或分页限制。
- 图片移入回收站时，笔记改为引用独立配图条目，共享原文件；新建和编辑笔记不能引用已删除图片，须先恢复。升级及旧备份恢复自动修复旧引用。
- 永久删除在事务内完成，保留共享文件；未引用原文件通过持久清理队列释放，失败和重启后自动重试。备份、导出与文件清理互斥。
- 数据库增量升级至 schema 7，新增清理队列表；已有备份保留。普通采集扩展保持 0.9.7，无须更新扩展。

## 0.9.7 · 视频首帧封面与视频专用嗅探

- 视频素材卡片和播放器使用原视频首帧作为封面；按需生成并复用有上限的内存缓存，原文件不变，已有视频、完整备份恢复后的文件同样适用。
- 浏览器资源嗅探只显示 MP4 / WebM / MOV / m3u8，图片请求不再进入列表，悬停图片下载、入库和成组浏览继续独立使用。
- 嗅探入库及平台链接采集尽量保留作品标题、作者、作者主页与来源；作者加入标签和 Markdown 备注，较准确的作品信息不会被网络文件名覆盖。
- 扩展更新为 0.9.7，保持扩展身份、原 API 连接和个人设置。

## 0.9.6 · 笔记自适应与图片组排序

- 修复笔记编辑 / 预览窗口仍然溢出的问题；按窗口高度分配正文空间，标题、工具栏和保存操作固定，长正文独立滚动。
- 图片组与笔记配图支持拖拽排序、前后移动和设为封面，第一张作为封面，保存后列表与连续预览即时更新。
- 笔记配图排序可选择同步正文图片顺序，保留文字、Markdown 图片说明及链接；关闭未保存排序时确认，版本冲突时整组回滚。
- 展示顺序与 Pixiv 原始页码分开保存，重复采集保持页身份与手动排序，新页追加到末尾；完整备份包含排序。数据库增量升级至 schema 6，不修改原文件。

## 0.9.5 · 自适应预览

- 图片 / 视频预览按可用窗口高度分配空间，图片完整等比适配，缩略图与保存操作始终留在窗口内。
- 收紧标题、标签、说明与关联笔记的留白；较矮桌面窗口自动并排显示知识库和标签，减少差一点就需要滚动的情况。
- 长说明和较小窗口只滚动详情区域，保存栏与失败提示保持可见；笔记配图预览同步适配窗口。
- 手机采用上下分区和固定操作栏，保留滚轮翻页、展开缩放、空白关闭与未保存提示。

## 0.9.4 · 日常操作优化

- 弹窗支持点击外部空白或按 Esc 关闭；嵌套预览只关闭当前层，展开图片也可点击图片外空白退出。
- 拖动选中文字、平移图片不会误关窗口；未保存的图文编辑保留确认提示，保存和上传等进行中的操作受保护。
- 选择模式下点击图片、标题或卡片正文即可选中 / 取消，勾选框与卡片状态同步，整张卡片显示选中边框；兼容键盘与触摸。
- 补充删除与引用说明：当前删除是移入回收站，笔记已有图片引用继续显示，原文件空间不会立即释放。

## 0.9.3 · 媒体采集 0.9.3 / Pixiv 增强版 1.2.1（19.4.1.6）

- 笔记配图在素材库折叠成组，正文图片可按阅读顺序连续预览；复用原图共享物理文件，旧笔记首次启动自动整理。
- 预览增加缩略图列、A/D 与左右方向键；图片区域滚轮翻页，点击展开后滚轮缩放、拖动平移、双击复位。
- 图片组可一次修改知识库归属；笔记配图组连同笔记移动。单独或批量移动笔记同样带上配图，冲突整次回滚。
- 修正暗色预览的标签 / 图标对齐、下拉选项与选择框对比度。图片说明支持 Markdown，隐藏生成的原图地址及重复来源。
- 标签查询默认仅限未分类；筛选、上传和详情编辑根据所选知识库独立获取标签。
- 媒体采集增加网站黑名单，覆盖图片预览、浮窗及图片 / 视频嗅探；自动排除 ZNote 自身，已打开页面同步生效。
- Pixiv 入库说明精简为作者链接与作品简介，作者标签、来源和页序仍保留；原下载与自动收藏流程不变。

## 0.9.2 · Pixiv 增强版 1.2.0（扩展 19.4.1.5）

- 入库按钮就地选择按作品分组或逐张入库；分组封面显示张数、按页浏览，选择模式展开各页，分组与页序随完整备份恢复。相同字节的不同页保留独立记录并共享原文件。
- 沿用原插件自动收藏、附带标签及公开 / 私密设置，文件入库成功后按作品收藏；收藏失败独立重试，关闭来源页后等待同账号 Pixiv 页面恢复同步。
- 原图、漫画各页、动图和小说自动加入作者姓名标签；优先保留作者标签，完整姓名及作者 ID / 主页链接仍保存在备注。

- 作品封面旁直接入库，默认 Z，自定义快捷键；详情页支持整部作品入库，无需跳转任务页。
- 原插件即时抓取按钮旁增加对应入库操作，复用作者、收藏、搜索等目录范围与筛选；只路由本次入库结果，保留普通下载和自动下载偏好。
- Pixiv 页内选择知识库、标签及查看队列，支持停止、继续与失败重试。后台持久队列在来源页关闭后继续，浏览器重启恢复。
- 支持 2000 个未完成任务、单任务 10000 个文件；分块交接元数据并按文件记录进度，保留最近 100 个完成任务。
- 固定扩展 ID 和原连接继续保留；补充 offscreen 权限用于 Markdown 解析、动图转换与后台入库。

## 0.9.1

- 扩展 UI 统一为石墨灰与蓝紫强调色：重做采集入口，统一悬浮预览、媒体页面、作品批量入库、连接设置及 Pixiv 入库任务页；保留网页自身的主题选择。
- 切换知识库 / 分类时取消旧列表请求并同步清理搜索、选择和详情状态；防止旧的连续浏览、翻页和深链接响应重新打开另一知识库的图片。
- 笔记配图组件保持稳定，临时加载失败可直接点击「重试图片」，无需刷新整页。
- Pixiv 增强桥接更新至 1.0.1（扩展 19.4.1.2），保持固定扩展 ID；覆盖更新后连接令牌和任务继续保留。


## 0.9.0

- 新增独立的 Pixiv 增强版：基于固定提交的 Powerful Pixiv Downloader 19.4.1，保留原抓取、筛选、下载与浏览功能，增加「保存抓取结果到 ZNote」。
- 入库任务支持作者 / 来源 / 页码 / 标签元数据、整组原图、APNG 动图及 Markdown 小说配图归档；独立于原下载记录，支持浏览器重启后继续、停止和失败重试。
- 小说新增按规范来源与知识库去重的 API，上传响应丢失后的重试不会重复建笔记，也不覆盖本地编辑。
- 设置页提供增强版安装包与完整 GPL 对应源码下载；构建脚本固定上游版本，保留原作者署名与许可证。

## 0.8.7

- 作品悬停预览新增「批量入库 N 张」；在独立作品页选择知识库和多个标签后，按页序保存整组原图。
- 入库保留作品详情链接、来源网站标签、原图地址和页码；下载与入库分别记录进度，支持失败重试、停止和刷新继续。
- 任务开始后固定目标知识库与标签，避免修改扩展默认设置导致一组图片分散入库；复用服务器按原图和知识库去重，处理上传成功但响应丢失后的重试。

## 0.8.6

- 小封面按作品链接 / 图片标识解析：支持 Pixiv 作者与推荐列表、Pawchive 作品列表；保持来源指向被选中的作品详情。
- 预览和原图分开：Pixiv 使用 regular 大图、Pawchive 使用网站缩略图，提供「查看原图」；下载与入库仍使用完整原文件。
- 仅缓存当前和相邻预览，提前加载上一张 / 下一张，合并重复请求并取消离开范围的请求；缺少轻量预览时不预下载相邻原图。
- 修复小图初始预览覆盖封面，以及遮罩封面被误判为鼠标离开而关闭的问题。

## 0.8.5

- 修复鼠标留在网页原缩略图上时无法滚轮翻页：原缩略图与展开预览均可翻页，其他区域仍正常滚动网页。
- 原图加载期间允许继续翻页，取消过时请求，防止慢速旧页覆盖最新选择；下载 / 入库仍对应实际显示的图片。
- 补充原缩略图、预览图中央、慢速连续翻页和页面正常滚动的浏览器回归测试。

## 0.8.4

- 悬停预览支持当前作品多图分组、页码、上一张 / 下一张、滚轮和左右方向键翻页。
- Pixiv 复用作品页原图列表，Pawchive 复用正文附件原图；排除其他作品和推荐卡片。其他站点支持当前文章或明确图库内的图片组。
- 批量下载在独立进度页按页序编号保存原文件，无需知识库令牌；支持停止、继续、失败重试及刷新恢复进度。每组最多 200 张，单张最多 25 MB。
- 保留单图下载 / 入库快捷键、预览尺寸、快速关闭和视频封面避让。

## 0.8.3

- 笔记保存默认归档 Markdown 外部配图，支持普通、尖括号及引用式图片；保留链接与代码。已有笔记可一键归档，失败可重试。
- 扩展正文归档提升到每次 200 张 / 500 MB；配图保存在所属知识库，原文件参与备份。
- 批量选择栏增加「删除所选」，回收站支持批量恢复；操作校验版本和知识库并整体提交。
- 全部内容、图片、视频、笔记、收藏、回收站、标签、统计和反向链接按当前知识库展示。
- Pixiv 作品正文读取全部页原图与说明；Pawchive 按作品正文原始文件链接采集，排除头像、推荐与缩略图。
- Pixiv 图片请求由扩展补充限定来源请求头，仅作用于本扩展对 pximg.net 的请求。

## 0.8.2

- 鼠标离开原图和预览后约 150 毫秒收起；仅在两者之间的通行间隙保留 650 毫秒，按钮仍可直接点击。
- 默认 S 下载、Z 入库，旧版 S/K 默认组合自动迁移；其他自定义组合保留，也可重新指定 K。
- 首页、标签筛选、外观、存储、备份、API 和采集设置的辅助说明改为问号提示，支持悬停、键盘和手机点击。
- 精简页头与筛选区，增大部分文字，优化手机操作按钮排列，更新主题截图。

## 0.8.1

- 改善大图预览的鼠标操作：预留从原图移向按钮的时间，进入操作区后取消待触发的预览切换。
- 下载/入库处理时禁用重复点击，并保持结果可读；操作按钮字号增大。
- 浏览器验证直接点击下载与入库、跨越预览间隙及在按钮上停留。
- 项目接入 GitHub 维护，重新整理 README、主题截图和部署/API/存储文档。
- 修正依赖锁文件元数据与新目录测试初始化，侧栏版本号改为读取 package.json。

## 0.8.0

- 预览大小可调并自动避开当前封面，验证与 B 站封面视频同时工作。
- 网页正文转换为 Markdown 图文笔记，默认归档配图，保留链接和来源。
- 固定扩展 ID，一键连接知识库；更新保留 API 连接和偏好。

## 0.7.0

- 自定义下载/入库快捷键，改善遮罩缩略图和背景图悬停。
- 自动来源标签、重复资源来源合并和可点击追溯。

## 0.6.0

- 悬停大图与图片/视频混合资源发现浮窗。
- MP4/m3u8 预览、下载、保存，HLS 分片无损合并。

## 0.5.0

- 平台视频链接解析与异步采集任务。
- 网络采集来源网址与时间记录。

## 0.4.0

- 视频上传、预览和 Range 播放；导出与备份包含视频。
- Chrome / Edge 高清图片右键采集。

## 0.3.0

- 跨知识库共享原图、连续浏览、批量整理。
- 自动备份与恢复、Webhook、主题风格和多标签筛选。

## 0.1–0.2

- 图片素材库、Markdown 笔记、局域网访问与 REST API。
- 批量上传、无损去重、夜间模式、默认知识库及多模式导出。
