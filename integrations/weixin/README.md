# 微信收件箱

在 ZNote「设置与连接 → 微信收件箱」选择知识库、填写标签，点击「扫码连接微信」，用手机微信扫码并确认。需要验证码时，在页面填入手机显示的验证码。随后向绑定的微信 ClawBot 会话发送内容。

| 发送内容 | 保存结果 |
| --- | --- |
| 文字 | Markdown 笔记，保留正文与链接 |
| 图片 | 本地图片素材，保存微信传来的原文件 |
| 同条消息中的文字与图片 | 图文笔记和本地配图组，按消息顺序排列 |

分开发送的消息分别入库，不按时间自动拼接。默认加入「微信」标签；自定义标签和知识库在收到消息时确定，之后修改默认设置不会改变已经排队的消息。目前不接收群聊、语音、视频及其他文件消息；不支持的消息会在收件记录中显示失败，不会假报保存成功。微信聊天图片没有原网页地址，不能凭空补出原作者或来源网址。

微信可能在发送前压缩图片，ZNote 保存的是微信实际提供的字节，不会恢复微信已丢弃的画质。图片仍遵循单张 25 MB 上限，同条消息最多 32 张。GIF 等动画若作为完整图片文件收到，按现有原图存储策略保留。

## 连接方式

使用腾讯 `openclaw-weixin` 公开的 iLink 通道协议。此实现直接调用该通道，不需要安装 OpenClaw、配置模型，或将 ZNote 暴露到公网。需要微信账号和客户端可使用 ClawBot 入口；真实可用性以手机扫码、绑定后接收测试为准。该入口不会读取普通好友会话和微信历史聊天。

ZNote 电脑保持开机并连接互联网时，通过一个可取消的长轮询连接接收消息；断网后逐步延长重试间隔。连接过期时暂停，提示重新扫码。「暂停接收」保留连接；「断开连接」移除本地连接凭据。不会自动向微信发送回复。

只接收扫码绑定账号发送的消息，过滤其他发送者、机器人消息及群消息。扫码、连接设置、状态和重试接口只允许 ZNote 管理员使用；现有 `read`、`write` API 令牌不能读取微信状态或连接凭据。

## 恢复与备份

游标和接收队列一起持久化，成功后移除队列内的原始消息与图片解密信息。最多保存 100 条未完成消息及最近 400 条已处理记录，设置界面展示最近 30 条。失败项可以重试或忽略；链接过期时可能需要从微信重新发送。停止期间未能从微信接收到的消息受微信平台保留期限约束，不保证无限期补收。

内容使用由账号、发送者、消息编号和图片页序派生的稳定 ID。重复推送或异常重启不重复生成已完成内容，也不会恢复用户已删除的记录。不同消息中的相同图片各自保留归属，但共享原文件。

连接配置、游标和收件记录保存在现有数据库 `settings` 中，键为 `weixin_inbox_v1`；未连接时不新增配置。完整备份和恢复包含此配置以及入库原文件，普通内容导出不包含连接信息。迁移时停用旧实例的收件连接后再启用新实例，避免两台电脑使用同一连接竞争收件。

## 协议依据与许可

参考 [Tencent/openclaw-weixin](https://github.com/Tencent/openclaw-weixin)，核对提交 `7c04adc3e95775efd661ab9fba0626d86d237713` 的 [协议说明](https://github.com/Tencent/openclaw-weixin/blob/7c04adc3e95775efd661ab9fba0626d86d237713/docs/protocol.md)、二维码登录、HTTP 请求和 CDN 解密实现。腾讯说明中的类型字段并不等于完整的服务端契约，后续接口变化需继续以真实登录和收件验证。

协议实现和媒体密钥解码参考腾讯 MIT 代码，许可保存在 [TENCENT-LICENSE](TENCENT-LICENSE)。二维码在浏览器内由 MIT 许可的 [node-qrcode](https://github.com/soldair/node-qrcode) 生成，不向第三方二维码服务发送登录内容。

## 验证

```powershell
node --test tests/weixin.test.js
npm run build -- --outDir artifacts/build-v0929
node tests/weixin-ui.mjs
```

覆盖腾讯两种媒体密钥编码、64 位消息编号精度、CDN 请求不携带账号凭据、消息去重、配图分组、来源标签、知识库隔离、部分失败、重放、完整备份恢复及 Edge 扫码/验证码界面。浏览器测试使用受控微信服务；真实账号仍需本人扫码确认后发送文字和图片验收。
